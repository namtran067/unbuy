services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=file:/data/custom.db
      - NODE_ENV=production
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
