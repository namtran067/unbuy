services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=file:/data/custom.db
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
